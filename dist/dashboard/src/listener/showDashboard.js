import { KEY_IS_SHOW_DASHBOARD } from "../contants";

export default function showDashboardListener(hide, show) {
  // GM_addValueChangeListener(KEY_IS_SHOW_DASHBOARD, (_, __, newVal, remote) => {
  //   if (!remote) return;
  //   if (!newVal) {
  //     hide();
  //   }
  //   if (newVal) {
  //     show();
  //   }
  // });
}
